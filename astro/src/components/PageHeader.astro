---
interface Props {
	header: string
	description?: string
}

const { header, description } = Astro.props
---

<h2 class="text-highlight my-4 text-center text-4xl font-bold">
	{header}
</h2>
{description && <p class="text-center text-lg">{description}</p>}
